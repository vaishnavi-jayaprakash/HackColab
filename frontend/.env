# Base URL for the backend API. Point this at your teammate's backend when it's ready.
VITE_API_BASE_URL=http://localhost:8000/api
# Set to "true" to use mock data instead of calling the real backend.
VITE_USE_MOCK=true
